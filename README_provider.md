# Provider依赖安装说明

为了使番茄钟功能正常工作，您需要添加provider依赖包。请运行以下命令：

```bash
flutter pub add provider
```

或在pubspec.yaml文件中手动添加以下依赖：

```yaml
dependencies:
  provider: ^6.1.1
```

然后运行：

```bash
flutter pub get
```

来获取依赖包。 